import './NavBar.css'

export default function NavBar() {
    return (
        <nav className='navbar'>
            <a href="#">Home</a>
            <a href="#">Tech Stack</a>
            <a href="#">Work History</a>
            <a href="#">Projects</a>
        </nav>
    );
}